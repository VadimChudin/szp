# Smart Zones Pro 7.0

Based on experimental 6.0.3-rc1 (`2f10c24`). Zone math is unchanged.

## Display
Price labels sit on the right scale as `OBJ_LABEL`, not on candles.
Close levels are spaced in pixels so numbers do not overlap or disappear in wicks.

## Verification
Python display-contract tests passed. MetaTrader visual check is still required after install.
